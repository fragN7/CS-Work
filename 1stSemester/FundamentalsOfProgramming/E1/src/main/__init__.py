from src.board.Board import Board
from src.move.Move import Move
from src.validation.Validation import Validation
from src.ui.Interface import UserInterface


if __name__ == '__main__':
    # board = Board()
    # validation = Validation(board)
    # move = Move(board, validation)
    # user = UserInterface(board, move)
    # user.run()
    pass

